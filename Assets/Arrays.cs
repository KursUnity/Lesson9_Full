using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Arrays : MonoBehaviour
{ // ћассивы... ”прощают создание многочисленных переменных. ≈сли переменных 1000, то не нужно создавать все 1000 вручную
  // „то бы создать массив, достаточно после типа переменных поставить квадратные скобки

    //public float[] Axis;

    void Update()
    {
        //if (Input.GetMouseButtonDown(0))
        //{
        //    transform.position = new Vector3(Axis[0], Axis[1], Axis[2]);
        //}
    }

    // Ёлементы массива начинаютс€ с нул€
    // Axis[0] - Ёто обращение к первому элементу массива
}
